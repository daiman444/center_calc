how to install.

open a terminal in the folder with the program and execute:

pip install -r requirements.txt

after installing the packages in the folder with the program, execute:

python3 center_calculator.py